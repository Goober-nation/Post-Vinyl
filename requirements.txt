# Core dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
requests>=2.32.0
python-dotenv>=1.0.0
toml>=0.10.2

# Database
# (SQLite is built-in, no dependency needed)

# Testing
pytest>=8.3.0
pytest-cov>=5.0.0
httpx>=0.27.0  # Required by FastAPI TestClient

# Linting
ruff>=0.7.0

# Type checking
mypy>=1.13.0

# Audio metadata (for future MBID extraction)
mutagen>=1.47.0

# Import/tagging (P6.6) — invoked as the `beet` CLI subprocess by
# app/services/beets.py, not imported as a Python library.
beets>=2.2.0
